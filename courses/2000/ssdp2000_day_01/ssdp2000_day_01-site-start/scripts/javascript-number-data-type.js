// JavaScript Number Data Type v1.0
// by: Michael Whyte
// c. 2025

// Grab all the output paragraphs
const p01 = document.getElementById('p-01');
const p02 = document.getElementById('p-02');
const p03 = document.getElementById('p-03');
const p04 = document.getElementById('p-04');
const p05 = document.getElementById('p-05');
const p06 = document.getElementById('p-06');
const p07 = document.getElementById('p-07');
const p08 = document.getElementById('p-08');
const p09 = document.getElementById('p-09');
const p10 = document.getElementById('p-10');

// JavaScript Numbers

// Numbers are numbers in JavaScript
const numA; // number data type
const numB; // number data type
// Output
//p01.innerHTML = numA;
//p02.innerHTML = numB;

// Math Operations
const numC = 51;
const numD = 27.78;
const numE = 18;
const numF = 5;
const numG = 2;
const numH = 9;

// Add
//p03.innerHTML = `${numC} &#43; ${numD} &equals; ${}`; // 78.78

// Subtract
//p04.innerHTML = `${numC} &#45; ${numD} &equals; ${}`; // 23.22

// Multiplication
//p05.innerHTML = `${numC} &times; ${numD} &equals; ${}`; // 1416.78

// Division
//p06.innerHTML = `${numC} &divide; ${numD} &equals; ${}`; // 1.8718502519798415

// Modulus

// Dividend is Larger than the Divisor
// - If the dividend (first number) is larger 
//   than the divisor (the second number) than 
//   the modulus operator will return the 
//   remainder. See below.</p>
//p07.innerHTML = `${numE} &#37; ${numF} = ${}`; // 3

// Dividend is Smaller than the Divisor
// If the dividend (first number) is smaller 
// than the divisor (the second number) then 
// the modulus operator returns the 
// dividend number. See below.
//p08.innerHTML = `${numG} &#37; ${numH} = ${}`; // 2

// Order Of Operations
// - JavaScript follows standard order of operations
// -- BEDMAS
// --- B: Brackets
// --- E: Exponents
// --- D: Division
// --- M: Multiplication
// --- A: Add
// --- S: Subtract

// - You will get different results with the same data if your math
//   operation is written differently
const numI = 15
const numJ = 3;
const numK = 5;

const totalA; // 30
// Output
//p09.innerHTML = `${numI} &#43; ${numJ} &times; ${numK} &equals; ${totalA}`; 

const totalB;
// Output
//p10.innerHTML = `&#40;${numI} &#43; ${numJ}&#41; &times; ${numK} &equals; ${totalB}`;


